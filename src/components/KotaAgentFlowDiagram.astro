---
import MermaidDiagram from './MermaidDiagram.astro';
---

<MermaidDiagram
  name="kota-request-flow"
  label="Flow diagram: a plain-language request is classified and routed to a quick action, a multi-step task, or a compliance question. Each path pulls real numbers from records, prepares a reviewable draft, then checks whether the action touches real data. Anything sensitive is held for approval before completing, and the result is remembered for next time."
  caption="How any request moves through Kota: routed, backed by real numbers, drafted, reviewed if it touches real data, then remembered."
/>
