---
title: "Enterprise Design System: Architecture & Adoption"
description: "Architected the component library, token model, and governance that let distributed financial-services teams build on one foundation across web and Flutter."
ai_summary: "Design system architecture for an enterprise financial institution: atomic component library, design token model, cross-platform adaptation for React and Flutter, documentation, and the governance model that drove adoption across distributed teams. Companion case study to the Banking Platform creative direction project."
image: "/images/card 01 - design.webp"
tags: ["Design Systems", "Design Tokens", "Governance", "Documentation"]
use_for_questions: ["Design systems", "Design tokens", "Component libraries", "Documentation", "Governance", "Cross-team adoption", "Atomic design"]
published: true
order: 2
accent: "#1898B5"
accentForeground: "#F1FAFC"
outcome: "A production design system: components, tokens, and governance, that distributed teams built on across web and mobile."
tldr:
  problem: "Distributed teams were rebuilding the same UI differently on every project, with no shared foundation and no way to agree on what 'correct' looked like."
  built: "An atomic component library, a design token model that survived both React and Flutter, and the documentation and governance to make teams actually adopt it."
  result: "One shared foundation across platforms, and a contribution model that kept the system alive after handover."
stats:
  - label: "Role"
    value: "Senior UI Designer"
  - label: "Focus"
    value: "System Architecture"
  - label: "Output"
    value: "Web & Flutter System"
  - label: "Scope"
    value: "Distributed Teams"
---
import ClickableImage from '../../components/ClickableImage.astro';
import PlaceholderImage from '../../components/PlaceholderImage.astro';
import FeatureCard from '../../components/FeatureCard.astro';
import TechStack from '../../components/TechStack.astro';

<div class="prose prose-lg max-w-none">

> **Companion case study:** this piece covers the system itself: architecture, tokens, documentation, and adoption. The creative direction and product UI it supported are covered in [Banking Platform: Creative Direction](/projects/financial-institution-digital-transformation).
>
> **NDA note:** this is an NDA-protected project. The process described is real; visuals are representative.

## Context

Inside a financial institution running several digital products, every new project started by rebuilding the same things: buttons, form fields, error states, navigation. Each team solved them slightly differently. The cost showed up twice: once in duplicated effort, and again in a customer experience that felt assembled rather than designed.

The brief was a design system. The actual problem was that teams had no agreed definition of correct, and no cheap way to find one.

## The Problem

**No shared foundation.** Multiple products, each with its own interpretation of the brand. Nothing was reusable because nothing was authoritative.

**Two platforms, different physics.** The system had to serve React on web and Flutter on mobile. Flutter's widget model doesn't map cleanly onto web component conventions, so a naive shared library would break the moment it met real mobile code.

**Distributed teams.** Designers and engineers sat across departments and vendors. Anything requiring a meeting to interpret was going to fail.

**Speed pressure.** Delivery timelines assumed the system would accelerate teams immediately, not after a six-month adoption curve.

## My Role

I owned system architecture. I designed the component library and token model, wrote the documentation, defined the contribution and naming standards, and worked directly with engineers on both platforms to make sure the system held up in code rather than only in Figma.

{/* TODO(nasif): this section needs scale. Even approximate figures transform it:
      - how many components in the library at handover?
      - how many teams / products consumed it?
      - how many tokens, or how many themes did it support?
    "A design system" is a category. "62 components across 4 product teams" is a credential. */}

## Process

### 1. Foundation: tokens before components

I started with design tokens rather than components, because tokens are what make a system survive two platforms. Colour, typography, spacing, and elevation were defined as named values with no platform assumptions baked in, then mapped separately into React and Flutter.

Components came second, structured on atomic design: atoms, molecules, organisms, so teams could compose rather than request.

<ClickableImage
  src="/images/card 03 - base components.webp"
  alt="Base component library showing atoms and molecules"
  caption="The base layer: atoms and molecules built to compose, so teams could assemble patterns without new design work."
/>

<PlaceholderImage
  title="The token architecture"
  why="Show the actual token layers: primitives, semantic aliases, component-level tokens, and how one value resolves down through them. This is the most senior-signalling artifact in the case study and there's currently nothing visual proving you did it."
  caption="Token architecture: primitives feed semantic aliases, which resolve into component tokens per platform."
/>

### 2. Structure and naming

Naming was the highest-leverage decision in the project. A distributed team can't ask clarifying questions at scale, so names had to be self-explanatory and predictable enough that engineers could guess correctly.

<ClickableImage
  src="/images/card 02 - structure.webp"
  alt="Design system structure and hierarchy"
  caption="System structure: how atoms compose into molecules and organisms, and where teams are expected to extend rather than fork."
/>

### 3. Platform adaptation

For mobile I reworked components against Flutter's constraints rather than forcing web patterns onto it. Where a pattern couldn't survive translation, I designed the platform-appropriate equivalent and documented the mapping, so a designer and a Flutter engineer would still agree they were looking at the same component.

I also built a notification framework covering how the products communicate state and errors, a repeated decision turned into a rule.

<PlaceholderImage
  title="One component, both platforms"
  why="Take a single non-trivial component, a form field with validation states, or a date picker, and show the React version beside the Flutter version, with the divergences annotated. This proves you understand implementation, which is exactly what separates a senior systems designer from a component librarian."
  ratio="16/9"
  caption="The same component in React and Flutter. Annotated where the platforms had to diverge, and why."
/>

### 4. Documentation and adoption

A system nobody adopts is a folder. I wrote usage guidelines, do/don't examples, and contribution rules, then ran feedback loops with the teams consuming it to find where the system was fighting them.

<PlaceholderImage
  title="A documentation page as teams actually saw it"
  why="Screenshot one real component's documentation: anatomy, states, usage rules, code reference. Documentation is claimed here but never shown, and it's a large part of what the role actually was."
  caption="A component's documentation page: anatomy, states, usage rules, and platform notes."
/>

{/* TODO(nasif): adoption evidence is the weakest link. Anything here helps:
      - how many teams adopted it, and over what period?
      - did anyone contribute components back? (that's the strongest possible signal)
      - any before/after on time-to-build a standard screen?
      - did it outlive your involvement? */}

## Key Decisions

**Tokens first, components second.** Building components first would have hard-coded web assumptions that Flutter couldn't honour. Starting at the token layer meant one source of truth and two honest translations.

**Documented divergence over forced parity.** Pretending React and Flutter can render identical components produces a system engineers quietly abandon. Naming the divergences kept the system trustworthy.

**Guessable names over precise ones.** I optimised naming for what a distributed engineer would search for, not for taxonomic correctness.

## Outcome

**One foundation across platforms.** Web and mobile products built from the same token source and a shared component vocabulary, replacing per-project reinvention.

**Faster standard work.** Teams composed common screens from existing components instead of designing and building them from scratch each time.

**A system that could be extended.** Contribution rules and naming conventions meant teams could add to the system without forking it.

{/* TODO(nasif): replace the three claims above with measured versions if you can
    get anywhere near a number. As written they are still assertions, and a design
    director will read them as unverifiable. Even "roughly halved the time to build
    a standard form screen, per the front-end lead" is dramatically stronger. */}

## Technical Implementation

<TechStack
  items={[
    { category: "Methodology", tools: ["Atomic Design", "Design Tokens"] },
    { category: "Tools", tools: ["Figma", "Documentation Platform"] },
    { category: "Frameworks", tools: ["React (Web)", "Flutter (Mobile)"] },
    { category: "Deliverables", tools: ["Component Library", "Usage Guidelines", "Contribution Model"] }
  ]}
/>

## Reflection

**What worked.** Involving engineers from the start. Every component I designed in isolation needed rework; every one I designed alongside the people implementing it shipped close to intact.

**What I'd do differently.** I treated documentation as a phase rather than a product. It should have shipped alongside each component from day one, because undocumented components get used incorrectly and then get blamed.

**What I learned about governance.** The hard part of a design system isn't the components. It's holding the line between flexible enough to solve real problems and consistent enough to be worth having. That balance is a standing negotiation, not a decision you make once.

</div>
