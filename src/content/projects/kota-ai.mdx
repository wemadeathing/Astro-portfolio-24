---
title: "Kota AI - Designing an Operational AI Workspace"
description: "Designed and built a multi-agent AI operations platform for South African SMEs, solo, from problem framing to working product."
ai_summary: "A product design and AI systems case study about Kota, an operational workspace for South African SMEs. Covers problem framing, build-in-code design process, generative UI strategy, multi-agent architecture, and the UX journey for turning plain-language intent into usable business outputs."
image: "/images/work/kota/kota-feature.webp"
tags: ["Product Design", "AI Systems", "Operational UX", "Generative UI", "Solo Build"]
use_for_questions: ["Product design", "AI product design", "Generative UI", "Operational UX", "Multi-agent architecture", "Designing in code", "South African SME software", "Solo building with AI tools"]
featured: true
published: true
order: 3
accent: "#EDE6D8"
accentForeground: "#16382E"
outcome: "An AI operations workspace for South African SMEs, designed and built end to end, solo, now in pre-launch beta."
tldr:
  problem: "SME owners lose momentum to admin. Quotes go out late, invoices sit unsent, and every tool needs the business explained to it again."
  built: "An AI workspace that already knows the business and turns plain language into usable output: invoices, proposals, follow-ups, next actions."
  result: "A working multi-agent product in pre-launch beta, designed in code and built solo, with reliability favoured over AI theatrics."
stats:
  - label: "Role"
    value: "Designer & solo builder"
  - label: "Approach"
    value: "Designed in code"
  - label: "Type"
    value: "Multi-agent AI product"
  - label: "Status"
    value: "Pre-launch beta"
---
import ClickableImage from '../../components/ClickableImage.astro';
import PlaceholderImage from '../../components/PlaceholderImage.astro';
import FeatureCard from '../../components/FeatureCard.astro';
import TechStack from '../../components/TechStack.astro';
import KotaAgentFlowDiagram from '../../components/KotaAgentFlowDiagram.astro';
import KotaInvoiceJourneyDiagram from '../../components/KotaInvoiceJourneyDiagram.astro';
import KotaBetaBanner from '../../components/KotaBetaBanner';

<div class="prose prose-lg max-w-none">

## Context

For many South African service businesses, the drag isn't the work — it's everything around it. Quotes go out late. Invoices sit unsent. Follow-ups slip. Proposals get rewritten from scratch. Context lives across memory, WhatsApp threads, notes, and half-finished documents.

Most tools treat these as separate workflows. In practice they're one problem: the same person doing the work is also the back office, and the admin arrives at the worst possible moment.

Kota is my attempt to design around that.

{/* TODO(nasif): this is the biggest credibility gap in the case study. Right now
    the problem is asserted from personal experience. Even lightweight evidence
    changes the read completely:
      - did you talk to any SME owners before building? how many, what did they say?
      - one verbatim quote from a real owner would carry this whole section
      - any survey, WhatsApp poll, or informal interview notes you can cite?
    If it genuinely came only from your own experience, say that explicitly and
    own it — "I built this for a problem I had" is honest and defensible.
    Unsourced claims about a market are not. */}

## The Problem

**Admin breaks momentum.** Important tasks don't fail because they're hard. They fail because they arrive at the wrong time, with too much friction.

**Operational context doesn't compound.** A proposal doesn't know your pricing. A draft message doesn't know your tone. A quote doesn't know your VAT status. The business re-explains itself to every tool, every time.

**AI usually adds a layer instead of removing one.** A generic assistant answers questions. That isn't the same as moving work forward. In this category, a paragraph is rarely enough — the output has to become a usable artifact.

That produced the central design question:

> What does an AI product look like when it's designed around operational follow-through rather than conversational novelty?

## My Role

Everything: problem framing, product definition, UX, interface design, brand, system architecture, and implementation. Solo, end to end.

## Process

### Designing in code, not Figma

I didn't design this in Figma first. The product was built through AI-assisted coding workflows — Codex, Cursor, Claude Code — with brand and visual assets made in Affinity. The design process happened inside the build.

The loop was consistent: **build, test, reduce, iterate.** I went deliberately wide early, exploring a much larger feature surface than the final product needed, then cut. If a flow introduced friction, it got simplified. If a feature was impressive but didn't improve the task, it got removed.

The real discipline wasn't generation. It was reduction.

<PlaceholderImage
  title="What got cut — the feature surface, before and after"
  why="You describe going wide then cutting hard, but there's no evidence of it. Show the original feature map with the removed items struck through, or three screenshots of flows you killed. This is the clearest proof of design judgement in the whole case study, and right now it's only claimed."
  caption="The original feature surface against what shipped. Most of the work was deciding what to remove."
/>

<ClickableImage
  src="/images/work/kota/kota-dashboard.webp"
  alt="Kota dashboard showing the operational workspace interface"
  caption="The dashboard brings guidance, operational signals, and next actions into one calm workspace view."
/>

<PlaceholderImage
  title="Core product screens — 4 to 6 of them"
  why="This is a product design case study with one screenshot in it. Reviewers need to see the actual interface: the chat surface, a generated invoice, the client context view, the finance section. Without screens, the case study reads as an essay about AI rather than evidence you can design."
  ratio="16/9"
  caption="Core surfaces: conversation, generated documents, client context, and finance."
/>

### Deciding what the system returns

The strongest product decision wasn't technical. It was that the product should return **finished operational outcomes** rather than assistance.

That led to a small set of rules:

**Return work, not answers.** Ask for an invoice and you get an invoice draft to review and send — not a paragraph about invoicing.

**Let context compound quietly.** The system remembers business name, tone, pricing, payment terms, services, and compliance posture, so the user never restates them.

**Keep human review visible.** Anything sensitive or mutating stays explicit. Active, never reckless.

**Respect the surface.** A follow-up request inside communications isn't the same as one inside finance. The system should know which room you're in.

**Prefer reliability to theatrics.** Where a deterministic system beats an agentic one, the deterministic system wins.

### Architecture in service of the interaction

Under the interface, Kota runs a small multi-agent system rather than one general assistant: a router to classify intent, a general agent for single-step tasks, a workflow agent for multi-step outputs, and a South African compliance specialist.

The architecture is deliberately narrow. Separate agents only earn their place when the reasoning pattern is genuinely different — otherwise complexity grows faster than usefulness.

The other key decision was the **Generative UI contract**: the agent never generates interface code. It returns typed structured responses, and the frontend renders the matching component. That's what keeps the UX purposeful instead of chat-shaped by default.

<KotaAgentFlowDiagram />

### The invoice journey

<KotaInvoiceJourneyDiagram />

The goal was to collapse friction without hiding process. The user stays in control; the blank page disappears.

<PlaceholderImage
  title="The invoice flow as real screens"
  why="The diagram explains the flow abstractly. Put the actual UI beside it — the request typed in, the structured draft returned, the review state, the sent confirmation. Diagrams show you can think; screens show you can design."
  ratio="16/9"
  caption="Plain-language request through to reviewable invoice draft, in the real interface."
/>

## Key Decisions

**Structured responses over free-form generation.** Typed contracts meant the UI stayed predictable and debuggable, at the cost of flexibility. For an operations tool handling money, that trade was obvious.

**Four agents, not twelve.** Every additional agent adds routing ambiguity and failure modes. I kept the roster to reasoning patterns that were actually distinct.

**South African context designed in, not localised later.** ZAR, VAT, EFT workflows, and local compliance language shaped the data model from the start rather than being retrofitted.

## Outcome

Kota has advanced from an architecture-led concept to a resolved product system, currently in pre-launch beta.

<div class="grid grid-cols-1 sm:grid-cols-2 gap-6 my-8">
  <FeatureCard title="Product Thinking Led">
    Built around reducing admin friction and turning intent into usable output, rather than showcasing AI for its own sake.
  </FeatureCard>
  <FeatureCard title="Designed In Code">
    Shaped through building, testing, trimming, and iterating with AI-assisted tools instead of a Figma-first process.
  </FeatureCard>
  <FeatureCard title="System Design In Service Of UX">
    The multi-agent architecture, structured responses, and deterministic rules all exist to support clearer, more trustworthy workflows.
  </FeatureCard>
  <FeatureCard title="Market-Specific Execution">
    VAT, EFT, compliance language, and South African operational habits were designed in from the start.
  </FeatureCard>
</div>

{/* TODO(nasif): "pre-launch beta" is doing a lot of work here and it's the first
    thing an interviewer will push on. Before this goes out, add whatever is true:
      - how many beta users are in it? even 3 counts, if you say 3
      - one quote from someone who isn't you
      - anything you changed because a real user hit a problem
      - a date: "in beta since X" — an undated pre-launch reads as stalled
    If there genuinely are no users yet, reframe the outcome honestly as a
    build-and-architecture case study. That's a legitimate story. Implying
    validation you don't have is the thing that costs you the interview. */}

## Reflection

**What worked.** Deciding the output format before the interaction model. Once "return a usable artifact" was fixed, most UX questions answered themselves.

**What I'd do differently.** I built the architecture before I'd tested the core interaction with anyone. It held up, but that was partly luck — I'd now put a rough version in front of three business owners before committing to a multi-agent structure.

**What I'm still unsure about.** Whether the chat-led entry point is right. It's the fastest way in for someone who knows what they want, and the worst way in for someone who doesn't. That's an open question I'd want real usage data to settle.

<KotaBetaBanner />

</div>
